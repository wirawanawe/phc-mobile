# PRODUCTION .env.local TEMPLATE
# Copy this content to your production server's .env.local file

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=YOUR_MYSQL_ROOT_PASSWORD_HERE
DB_NAME=phc_dashboard
DB_PORT=3306

# JWT Secret (MUST be at least 32 characters)
JWT_SECRET=supersecretkey123456789supersecretkey123456789supersecretkey

# Application Configuration
NODE_ENV=production
PORT=3000

# Next.js Configuration
NEXT_PUBLIC_API_URL=https://dash.doctorphc.id/api

# INSTRUCTIONS:
# 1. Replace YOUR_MYSQL_ROOT_PASSWORD_HERE with your actual MySQL root password
# 2. You can get the MySQL root password from your server panel (aaPanel, cPanel, etc.)
# 3. Alternative: Create a dedicated user instead of using root:
#    DB_USER=phc_user
#    DB_PASSWORD=your_dedicated_user_password

# SECURITY NOTE:
# Make sure to set proper file permissions:
# chmod 600 .env.local
# chown your-app-user:your-app-user .env.local
